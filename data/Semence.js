class Semence{
    constructor(nom_francais, nom_latin, nom_semencier) {
        this.nom_francais = nom_francais;
        this.nom_latin = nom_latin;
        this.nom_semencier = nom_semencier;
    }
}

module.exports = Semence;